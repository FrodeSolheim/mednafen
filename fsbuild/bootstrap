#!/bin/sh
# This file is automatically generated by fs-package

set -e

if [ -f fsbuild/bootstrap.inc.sh ]; then
. fsbuild/bootstrap.inc.sh
elif [ -f fsplugin/Makefile ]; then
make -C fsplugin bootstrap
elif [ -f ./bootstrap ]; then
./bootstrap
fi
